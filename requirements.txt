# Qwen3-ASR GGUF Server Dependencies
# ====================================
# Python 3.9+ recommended

# ─── Core ────────────────────────────────────────────────────────────────
numpy>=1.24.0
soundfile>=0.12.0
requests>=2.31.0

# ─── Server ───────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.0.0

# ─── Model Download ───────────────────────────────────────────────────────
huggingface-hub>=0.17.0

# ─── Installation Notes ───────────────────────────────────────────────────
#
# 普通安装:
#   pip install -r requirements.txt
#
# 前置条件:
#   1. Python 3.9+
#   2. llama.cpp 已编译（start.bat 使用 llama.cpp/build/bin/Release/llama-server.exe）
#   3. Vulkan SDK (用于 AMD GPU): https://vulkan.lunarg.com/
#   4. Visual Studio Build Tools (用于编译 llama.cpp)
#
# 如果 Vulkan 不可用，设置环境变量 ASR_ENABLE_VULKAN=false 使用 CPU 模式。
#
# 模型下载:
#   python download_model.py
#
